<UserControl x:Class="DecoratorPattern.Views.Views.OrderUpdateViewArea"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DecoratorPattern.Views.Views"
             xmlns:storeManagerView="clr-namespace:DecoratorPattern.Views.Views.StoreManagerViews"
             mc:Ignorable="d" 
             d:DesignHeight="325" d:DesignWidth="700">
    <Grid HorizontalAlignment="Right" Width="700">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="500"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <ListBox x:Name="ordersListed" Grid.Column="0"></ListBox>
        <storeManagerView:OrderDetailsView Grid.Column="1"></storeManagerView:OrderDetailsView>
    </Grid>
</UserControl>
